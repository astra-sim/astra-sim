# Analytical Network Input File

# Ring Topology
topology: [ Ring, Ring, Ring ]  # Ring, Switch, FullyConnected

# 8 NPUs
npus_count: [ 2, 2, 2 ]  # number of NPUs

# Bandwidth per each link (per direction)
# max observed busbw = tbd
bandwidth: [ tbd, tbd, tbd ]  # 

# Latency per each link
# calculated link_latency = x
latency: [ tbd, tbd, tbd ]  # ns (1/2)*link_latency
